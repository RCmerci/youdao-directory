#ifndef COMMANDLINEPRINT
#define COMMANDLINEPRINT
#include <stdio.h>
#include "GatherData.h"
//print the result at command-line.(simple version)
void clprint_simple(TRANS *trans);
//print the result at command-line.
void clprint_full(TRANS *trans);
#endif

